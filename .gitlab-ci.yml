stages:
  - test
  - notify

variables:
  PYTHONPATH: "."

default:
  image: python:3.12
  before_script:
    - pip install --upgrade pip
    - pip install -r requirements.txt || true

#  Основной тестовый джоб
run_autotests:
  stage: test
  script:
    - echo "🚀 Запуск автотестов Eflow..."
    - pytest tests/ --maxfail=1 --disable-warnings -q || true
  allow_failure: true     # <-- прод НЕ падает
  tags:
    - apps
  rules:
    - if: '$CI_PIPELINE_SOURCE == "pipeline"'        # запуск вручную из UI
      when: on_success
    - if: '$CI_PIPELINE_SOURCE == "web"'             # ручной Run pipeline
      when: on_success
    - if: '$CI_COMMIT_BRANCH == "main"'              # при пуше в main
      when: on_success
    - if: '$CI_MERGE_REQUEST_ID'                     # в MR
      when: on_success
 
notify_fail:
  stage: notify
  script:
    - echo "❗ Автотесты упали. Проверь отчёты."
  when: on_failure
  allow_failure: true